# 电商运营助手准生产级升级与验收说明

版本：V1.1
日期：2026 年 9 月 14 日
目标：把可运行演示 MVP 升级为能够展示企业工程方法的准生产级求职作品，同时保持 2 核 4 GiB 单 ECS 可运行。

## 1 定位与边界

“准生产级”表示关键业务链路真实、状态可靠、数据库可迁移、任务可追踪、部署可恢复、测试有证据；不表示已经具备多地域容灾、监管合规认证或大规模企业 SLA。

保留边界：不连接真实淘宝、京东、抖音广告扣费接口；使用模拟业务数据；媒体保存在 ECS 本地目录；公网演示暂用 HTTP IP；真实图片和视频调用产生模型费用。

## 2 本次工程化改造

| 领域 | 改造结果 |
| --- | --- |
| 数据库 | 引入 Alembic；新增向前迁移；Compose 通过一次性 migrate 服务阻断未迁移应用启动 |
| 任务提交 | API 在同一事务中保存任务、输入快照、Outbox、任务事件和审计记录 |
| 消息投递 | 独立 Dispatcher 使用数据库行锁领取 Outbox；投递失败按上限退避重试 |
| 消费幂等 | 任务仅能从 pending 条件领取；同一逻辑任务只有一个 running attempt；终态拒绝迟到覆盖 |
| 执行恢复 | attempt 保存租约、Worker、供应商任务标识和结构化错误；Dispatcher 检查租约过期任务 |
| AI 编排与契约 | 四类文本 AI 使用 LangGraph Typed State、条件边和 PostgreSQL Checkpointer；Pydantic 校验并最多修复一次 |
| 模块边界 | 后端按 api/core/domain/infrastructure/integrations/services/workflows/workers 分包，部署入口仅作兼容装配 |
| 架构治理 | ACP-0001 恢复 LangGraph，ACP-0002 批准目录重构；哈希、关键证据、目录和入口体积由守卫与 CI 检查 |
| 内容版本 | 生成内容不再覆盖旧行；每次创建新 revision，并精确关联 task_id |
| 视频 | 供应商 submit 与 poll 拆分；轮询通过 Celery countdown 调度，不在 Worker 内 sleep |
| 媒体安全 | 仅允许 HTTPS 公网地址；校验 DNS 地址、MIME、扩展名、文件大小和空文件；先写临时文件再原子替换 |
| 认证 | JWT 加入 token_version；退出登录后旧 Token 失效；生产环境拒绝默认 APP_SECRET_KEY |
| 可观测 | 请求 ID、结构化 HTTP 日志、任务事件、审计日志、模型调用状态/模型/耗时元数据 |
| 入口安全 | Nginx API/登录限流、CSP 和常用安全头、日志轮转、no-new-privileges |
| 容器 | 后端镜像使用 UID 10001 非 root 用户；启动前显式准备 storage/backups 权限 |
| 运维 | 管理脚本新增 prepare、migrate、backup、doctor；update 先备份再更新 |

## 3 核心数据对象

- `generation_tasks`：逻辑任务、输入快照、幂等键、请求 ID、模式和终态。
- `task_attempts`：每次执行、租约、Worker、provider_job_id、错误分类。
- `task_events`：创建、领取、供应商提交、轮询、成功、失败、取消时间线。
- `outbox_events`：业务事务与 Celery 投递之间的可靠桥梁。
- `model_invocations`：模型、模态、真实/模拟模式、提示词/Schema 版本、状态和耗时。
- `audit_logs`：任务创建/取消、投放创建/提交/审批、退出等关键操作。
- `content_documents.task_id`：诊断、创意和复盘任务到确切内容修订的关联。
- `experiments.task_id`：AI 投放建议任务到可提交审批草稿的唯一关联。
- LangGraph `checkpoints` 系列表：节点恢复状态，不承担审批和业务事实。

## 4 验收矩阵

| 需求 | 场景 | 验证层 | 通过证据 |
| --- | --- | --- | --- |
| NFR-09 | 数据库从现有结构升级到 head | ECS/PostgreSQL | `alembic current` 为 `20260914_0002 (head)` |
| NFR-10 | 创建任务同时产生 Outbox | 单元/数据库 | 同一事务后 task、outbox、event 均存在 |
| NFR-11 | 重复幂等请求与重复 Worker 消息 | 单元/数据库 | 同 key 同 task；attempt 数量为 1；迟到更新返回 false |
| NFR-12 | 查询任务时间线 | API/ECS | `/api/tasks/{id}/events` 返回 created、started、终态事件 |
| NFR-13 | 非法 AI JSON | 单元 | Pydantic 拒绝；LangGraph 最多一次 repair，任务不会伪成功 |
| NFR-14 | 视频异步执行 | Worker/真实供应商 | submit 保存 provider_job_id；poll 短任务最终保存并播放 MP4 |
| NFR-15 | 审计和请求追踪 | HTTP/API | 响应含 X-Request-ID；主管可查询审计记录 |
| NFR-16 | 入口和容器安全 | Nginx/Docker | 安全头存在；应用容器 UID 非 0；日志设置限制 |
| NFR-17 | 可恢复发布 | ECS | 发布前产生 PostgreSQL dump；doctor 全部通过 |
| NFR-18 | 四类 LangGraph 工作流 | 单元/ECS/真实供应商 | Typed State、条件边、节点轨迹、PostgreSQL checkpoint 和 task 关联产物均存在 |
| NFR-19 | 架构基线防漂移 | 本机/CI/ECS | `ARCHITECTURE_GUARD_PASS: ACP-0002`；文档或关键实现偏移时非零退出 |
| NFR-20 | 模块化目录 | 静态/导入/运行 | 八类目录存在，四个入口保持薄适配，模块迁移后测试与容器启动正常 |

## 5 测试命令

本机：

```powershell
cd backend
uv run python -m compileall -q app migrations
uv run pytest -q
cd ..
python scripts/check_architecture.py
cd frontend
pnpm build
```

ECS：

```bash
cd /root/myproject/e_commerce_operations_copilot
./manage.sh doctor
./manage.sh guard
docker compose exec -T backend id
docker compose exec -T worker id
docker compose exec -T dispatcher id
curl -i http://127.0.0.1/api/health
curl -i http://127.0.0.1/api/ready
```

## 6 回滚原则

代码回滚不能撤销已经发生的数据变化。每次 `update` 前保存 PostgreSQL custom-format dump；迁移采用向前修复策略。出现问题时停止外部写入，保留故障日志和数据库备份，回到已验证代码并在隔离数据库验证恢复后再切换。禁止对生产数据直接执行未经演练的 downgrade。

## 7 当前验收结果

当前本机基线：架构守卫通过 ACP-0002；后端编译通过；pytest 12 项全部通过且无警告；前端 TypeScript 与生产构建通过；Compose 配置通过；敏感信息扫描未发现密钥或私钥正文。ECS、真实模型和浏览器验收必须在部署 `2bdc1ec` 或其后续文档提交后填写，未执行的层级不得以本机测试代替。
